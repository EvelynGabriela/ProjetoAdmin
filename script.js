	/*function Login(){
		var usr=0;
		var usuario=document.login-inputs.usuario.value;
			usuario=usuario.toLowerCase();
		var senha=document.login-inputs.senha.value;
			senha=senha.toLowerCase();
		if (usuario=="admin" && senha=="admin") {
			window.location="CAMINHO BANCO"; done=1;
		}
		if (usr==0) { alert("Dados incorretos, tente novamente"); }
	}

*/
function click() {
    window.open('../Projeto/quemsomos.html', 'button1');
}